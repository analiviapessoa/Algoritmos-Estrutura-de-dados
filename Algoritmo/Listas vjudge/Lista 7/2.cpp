#include <bits/stdc++.h>
using namespace std;

int main(){

    int n,m,j;  //nº linahs, largura, nº vezes pra evitar os espinhos
    cin >> n >> m >> j;

    for (int i = 0 ; i < n ; i++){
        for (int j = 0 ; j < m ; j++){
            int chars;
            cin >> chars;
        }
    }

    return 0;
}